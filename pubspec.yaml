name: apex_dropdown
description: Apex-quality dropdown widgets for Flutter - controllerless, crash-proof, and memory-safe. Single, multi, and async search support with full model object handling.
version: 1.0.1
homepage: https://github.com/yourusername/apex_dropdown
repository: https://github.com/yourusername/apex_dropdown
issue_tracker: https://github.com/yourusername/apex_dropdown/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

topics:
  - dropdown
  - ui
  - widgets
  - forms
  - search

flutter:
